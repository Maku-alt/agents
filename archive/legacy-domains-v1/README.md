# Legacy domains v1

Snapshot preservado de la arquitectura inicial del repositorio antes de la migracion al catalogo TOML del 2026-08-02.

Incluye:

- `domains/decks`: flujo PowerPoint de cinco agentes Markdown;
- `domains/research`: researcher Markdown extenso;
- `domains/sql`: flujo SQL/Teradata acoplado a convenciones externas;
- `schemas`: contratos JSON de decks;
- `templates`: plantillas de decks y SQL.

Este arbol no se sincroniza a Codex ni representa el flujo vigente. Sirve para consultar decisiones historicas o rescatar un artefacto al reabrir un consumidor legado.
